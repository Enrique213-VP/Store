<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Store" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/blue_mi_nina</item>

        <!-- Color de la barra de notificación para versiones Lollipop (5.0) y superiores -->
        <item name="android:statusBarColor">@color/blue_mi_nina</item>

        <!-- Opcional: Color de la barra de notificación para versiones pre-Lollipop -->
        <item name="colorPrimaryDark">@color/blue_mi_nina</item>

        <item name="popupMenuBackground">@color/white</item>
    </style>

    <style name="Theme.Store" parent="Base.Theme.Store" />
</resources>